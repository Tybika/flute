[gd_resource type="Resource" script_class="TextData" format=3 uid="uid://m0yu4tk7lnwp"]

[ext_resource type="Script" uid="uid://b8ttwtxu3cyox" path="res://texts/text_data.gd" id="1_pdfkb"]

[resource]
script = ExtResource("1_pdfkb")
long_text = "[b]ThemeData class[/b]
Classe da biblioteca de Material Design no Flutter

Define a configuração do tema visual geral para um MaterialApp ou para uma árvore de componentes dentro do aplicativo. A propriedade do tema MaterialApp pode ser usada para configurar a aparência de todo o aplicativo. 

Exemplo:

class ThemeDataExampleApp extends StatelessWidget {
  const ThemeDataExampleApp({super.key});

  @override
  Widget build(BuildContext context) {
    final ColorScheme colorScheme = ColorScheme.fromSeed(
      brightness: MediaQuery.platformBrightnessOf(context),
      seedColor: Colors.indigo,
    );

	return MaterialApp(
    		title: 'ThemeData Demo',
      	theme: ThemeData(
        		colorScheme: colorScheme,
        		floatingActionButtonTheme: FloatingActionButtonThemeData(
          		backgroundColor: colorScheme.tertiary,
          		foregroundColor: colorScheme.onTertiary,
       		),
		),
      home: const Home(),
    );
  }
}"
metadata/_custom_type_script = "uid://b8ttwtxu3cyox"
